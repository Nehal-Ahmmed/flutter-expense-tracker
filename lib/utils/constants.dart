/// [Constants]
/// Helper class to store constant strings, asset paths, etc.
///
/// Compose Comparison: similar to a `Constants.kt` object.
class Constants {
  static const String appName = 'Expense Tracker';
  static const String currencySymbol = '\$';

  // Asset Paths (Make sure to add these to pubspec.yaml if you add real images)
  // For now we will use Icons or placeholders
}
